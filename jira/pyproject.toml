[project]
name = "jira-mcp-server"
version = "0.1.0"
description = "JIRA MCP server using FastMCP"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.2.0",
    "httpx>=0.27.0",
]

[project.scripts]
jira-mcp = "server:main"
